[package]
name = "twin"
version = "0.1.0"
edition = "2024"

[dependencies]
uom = { version = "^0.38", features = ["f64", "serde"] }
nalgebra = { version = "^0.34", features = ["serde"] }
serde_json = "1.0"
